# 📖 توثيق واجهة برمجة التطبيقات (API) - تطبيق العميل (Customer App)

هذا التوثيق مصمم خصيصاً لمطوري ومبرمجي تطبيق العميل، ويوضح بالتفصيل كيفية التخاطب مع الخوادم الخاصة بنظام "القريني" بكل سهولة واحترافية.

---

## 🌐 معلومات الخادم (Server Info)
- **الرابط الأساسي (Base URL):** `https://server3.albkreparfume.tech`
- **نوع البيانات المرسلة والمستقبلة:** `application/json`

---

## 🔐 1. المصادقة وحساب العميل (Authentication & Account)

جميع مسارات هذا القسم تبدأ بـ: `/api/customer`

### 1.1 تسجيل حساب جديد (Register)
`POST /register`

يقوم بإنشاء حساب جديد لعميل وتسجيل دخوله مباشرة.

**المعاملات (Parameters):**
| الحقل (Field) | النوع (Type) | مطلوب؟ | الوصف (Description) |
| ------------- | ------------ | ------ | ------------------- |
| `name` | String | **نعم** | اسم العميل بالكامل |
| `email` | String | **نعم** | البريد الإلكتروني (يجب أن يكون فريداً) |
| `password` | String | **نعم** | كلمة المرور (6 خانات على الأقل) |
| `phone` | String | لا | رقم الهاتف للتواصل |
| `address` | String | لا | عنوان السكن أو التوصيل الافتراضي |

**مثال على الطلب (Request):**
```json
{
  "name": "أحمد محمد",
  "email": "ahmed@example.com",
  "password": "password123",
  "phone": "0501234567",
  "address": "الرياض، حي العليا"
}
```

**الاستجابة الناجحة (201 Created):**
```json
{
  "message": "تم إنشاء الحساب بنجاح مرحباً بك في القريني!",
  "token": "eyJhbGciOiJIUzI1NiIsInR5...",
  "customer": {
    "id": 1,
    "name": "أحمد محمد",
    "email": "ahmed@example.com",
    "phone": "0501234567",
    "address": "الرياض، حي العليا"
  }
}
```

---

### 1.2 تسجيل الدخول (Login)
`POST /login`

تسجيل دخول العميل للحصول على `Token` الوصول.

**المعاملات (Parameters):**
| الحقل (Field) | النوع (Type) | مطلوب؟ | الوصف (Description) |
| ------------- | ------------ | ------ | ------------------- |
| `email` | String | **نعم** | البريد الإلكتروني المسجل |
| `password` | String | **نعم** | كلمة المرور |

**مثال على الطلب (Request):**
```json
{
  "email": "ahmed@example.com",
  "password": "password123"
}
```

---

### 1.3 طلب رمز استعادة كلمة المرور (Forgot Password)
`POST /forgot-password`

إرسال رمز (OTP) لبريد العميل لاستعادة حسابه.

**المعاملات (Parameters):**
| الحقل (Field) | النوع (Type) | مطلوب؟ | الوصف (Description) |
| ------------- | ------------ | ------ | ------------------- |
| `email` | String | **نعم** | البريد الإلكتروني المرتبط بالحساب |

**الاستجابة الناجحة (200 OK):**
```json
{
  "message": "تم إرسال رمز التحقق (OTP) إلى بريدك الإلكتروني بنجاح، الرمز صالح لمدة 15 دقيقة",
  "email": "ahmed@example.com"
}
```

---

### 1.4 إعادة تعيين كلمة المرور (Reset Password)
`POST /reset-password`

إنشاء كلمة مرور جديدة باستخدام رمز (OTP) المرسل للبريد.

**المعاملات (Parameters):**
| الحقل (Field) | النوع (Type) | مطلوب؟ | الوصف (Description) |
| ------------- | ------------ | ------ | ------------------- |
| `email` | String | **نعم** | البريد الإلكتروني للعميل |
| `otp` | String | **نعم** | الرمز المرسل المكون من 6 أرقام |
| `newPassword` | String | **نعم** | كلمة المرور الجديدة |

---

### 1.5 تغيير كلمة المرور (Change Password) 🔒
`POST /change-password`

يسمح للعميل بتغيير كلمة المرور الخاصة به من داخل التطبيق. *(يتطلب إرسال Token)*.

**المعاملات (Parameters):**
| الحقل (Field) | النوع (Type) | مطلوب؟ | الوصف (Description) |
| ------------- | ------------ | ------ | ------------------- |
| `currentPassword` | String | **نعم** | كلمة المرور الحالية |
| `newPassword` | String | **نعم** | كلمة المرور الجديدة |

---

### 1.6 جلب الملف الشخصي والطلبات (Get Profile) 🔒
`GET /me`

جلب بيانات العميل المسجل مع آخر 10 طلبات قام بها. *(يتطلب إرسال Token)*.

**الاستجابة الناجحة (200 OK):**
ترجع كائن `Customer` يحتوي على البيانات ومصفوفة `Orders`.

---

## 🛍️ 2. المتجر والمنتجات (Store & Products)

جميع مسارات هذا القسم تبدأ بـ: `/api`

### 2.1 قائمة المنتجات (Get Products)
`GET /products`

استرجاع قائمة بجميع المنتجات المتوفرة في المتجر.

**الاستجابة الناجحة (200 OK):** `[ { "id": 1, "name": "...", "price": 100, ... } ]`

### 2.2 الأقسام والتصنيفات (Get Categories)
`GET /categories`

استرجاع جميع أقسام المتجر (لتصفية المنتجات في التطبيق).

### 2.3 العروض النشطة (Get Offers)
`GET /offers`

استرجاع قائمة العروض (البنرات الإعلانية والخصومات) لعرضها في الشاشة الرئيسية.

---

## 🛒 3. الطلبات (Orders)

جميع مسارات هذا القسم تبدأ بـ: `/api`

### 3.1 إنشاء طلب شراء (Create Order)
`POST /orders`

إرسال سلة المشتريات لإنشاء طلب جديد.

**المعاملات (Parameters):**
| الحقل (Field) | النوع (Type) | مطلوب؟ | الوصف (Description) |
| ------------- | ------------ | ------ | ------------------- |
| `CustomerId` | Integer | **نعم** | معرف العميل (ID) |
| `paymentMethod` | String | **نعم** | طريقة الدفع (مثال: `cash`، `card`) |
| `shippingAddress`| String | **نعم** | عنوان التوصيل النهائي |
| `notes` | String | لا | أي ملاحظات إضافية من العميل |
| `items` | Array | **نعم** | مصفوفة تحتوي على المنتجات والكميات |

**مثال لمصفوفة المنتجات (items):**
```json
[
  { "productId": 2, "quantity": 3 },
  { "productId": 5, "quantity": 1 }
]
```

---

## 👔 4. التصاميم الخاصة وطلبات الجملة (B2B & Customs)

جميع مسارات هذا القسم تبدأ بـ: `/api`

### 4.1 طلب تصميم مخصص (Custom Design Request)
`POST /designs`

يتيح للعميل تقديم فكرة لتفصيل يونيفورم مخصص وتزويد الإدارة بالتفاصيل المطلوبة.

**المعاملات (Parameters):**
| الحقل (Field) | النوع (Type) | مطلوب؟ | الوصف (Description) |
| ------------- | ------------ | ------ | ------------------- |
| `title` | String | **نعم** | عنوان الطلب (مثال: تصميم مدرسة) |
| `description` | String | **نعم** | وصف التصميم، الألوان، والخامات المطلوبة |
| `phone` | String | **نعم** | رقم الهاتف للتواصل حول التصميم |
| `author` | String | **نعم** | اسم العميل مقدم الطلب |
| `image` | String | لا | رابط الصورة المرجعية إن وجدت |

**مثال على الطلب (Request):**
```json
{
  "title": "طلب تصميم يونيفورم مدرسة النور",
  "description": "نحتاج قمصان بيضاء وبنطلونات كحلية من القطن الصيفي...",
  "phone": "0501112223",
  "author": "مدرسة النور",
  "image": "https://example.com/logo.png"
}
```

---

### 4.2 طلب توريد للشركات / المدارس (B2B Request)
`POST /b2b/requests`

نموذج مخصص للشركات أو الجهات لطلب كميات كبيرة بأسعار خاصة بالجملة.

**المعاملات (Parameters):**
| الحقل (Field) | النوع (Type) | مطلوب؟ | الوصف (Description) |
| ------------- | ------------ | ------ | ------------------- |
| `companyName` | String | **نعم** | اسم الشركة، المدرسة، أو المؤسسة |
| `contactName` | String | **نعم** | اسم مسؤول التواصل (المناديب) |
| `phone` | String | **نعم** | رقم هاتف مسؤول التواصل |
| `itemType` | String | **نعم** | نوع النشاط (`school`, `medical`, `security`, `hotel`) |
| `quantity` | Integer | **نعم** | الكمية التقريبية المطلوبة |
| `estimatedTotal`| Decimal | لا | التكلفة التقديرية (يمكن حسابها لاحقاً) |

**مثال على الطلب (Request):**
```json
{
  "companyName": "مستشفى الأمل",
  "contactName": "د. عبدالله",
  "phone": "0509998887",
  "itemType": "medical",
  "quantity": 250
}
```

---

> [!IMPORTANT]
> **ملاحظة حول الحماية والمصادقة 🔒:**
> - المسارات المشار إليها برمز (🔒) تتطلب مصادقة (Authentication).
> - يجب إرسال رمز الـ JWT الذي يتم استلامه عند تسجيل الدخول في **Header** الطلب بالشكل التالي:
>   `Authorization: Bearer <your_jwt_token_here>`
